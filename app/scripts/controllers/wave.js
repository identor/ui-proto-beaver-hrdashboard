'use strict';

/**
 * @ngdoc function
 * @name leadScoreClientApp.controller:WaveCtrl
 * @description
 * # WaveCtrl
 * Controller of the leadScoreClientApp
 */
angular.module('leadScoreClientApp')
  .controller('WaveCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
